# Copy this file to `.env.semantic-cron` on the bigger server.
# Use the same CRON_SECRET configured in the Vercel deployment.
SEMANTIC_CRON_URL=https://your-production-domain.example/api/cron/hourly-semantic
CRON_SECRET=replace-with-the-vercel-cron-secret
